{% if include.posts.size >= 1 %}
<div class="related">
    <h2>{{ locale.related-posts }}</h2>
    <ul>
    {% for related in include.posts limit: 5 %}
        <li class="relatedPost">
            <a href="{{ related.url }}">{{ related.title }}
            {% if related.series %}
                ({{ locale.serie }}: {{ related.series }})
            {% endif %}
            </a>
        </li>
    {% endfor %}
    </ul>
</div>
{% endif %}
